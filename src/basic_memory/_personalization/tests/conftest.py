"""Pytest configuration.

The ``scripts`` package is installed (editable) into the environment,
so tests import it directly (e.g. ``from scripts import align_skills``)
with no ``sys.path`` manipulation required.
"""
